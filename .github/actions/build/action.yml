name: Build
description: Build and archive the app for distribution

inputs:
  apple-team-id:
    description: Apple Developer Team ID
    required: true
  package-token:
    description: GitHub token with read access to private Swift packages (PlatformRuntimeKit)
    required: true
  project-name:
    description: Xcode project file name
    required: false
    default: Thaw.xcodeproj
  scheme-name:
    description: Xcode scheme name
    required: false
    default: Thaw
  deployment-target:
    description: Minimum macOS deployment target
    required: false
    default: "26.0"

runs:
  using: composite
  steps:
    - name: Authorize private Swift packages
      shell: bash
      env:
        PACKAGE_TOKEN: ${{ inputs.package-token }}
      run: |
        set -euo pipefail
        printf 'machine github.com\nlogin x-access-token\npassword %s\n' "$PACKAGE_TOKEN" > ~/.netrc
        chmod 600 ~/.netrc
        git config --global url."https://x-access-token:${PACKAGE_TOKEN}@github.com/".insteadOf "https://github.com/"

    - name: Cache SwiftPM
      uses: actions/cache@v5
      with:
        path: ~/Library/Caches/org.swift.swiftpm
        key: spm-${{ runner.os }}-${{ hashFiles('**/Package.resolved') }}
        restore-keys: |
          spm-${{ runner.os }}-

    - name: xcodebuild archive
      shell: bash
      env:
        APPLE_TEAM_ID: ${{ inputs.apple-team-id }}
      run: |
        xcodebuild archive \
          -project "${{ inputs.project-name }}" \
          -scheme "${{ inputs.scheme-name }}" \
          -destination platform=macOS \
          -configuration Release \
          -archivePath build/Archive.xcarchive \
          MACOSX_DEPLOYMENT_TARGET=${{ inputs.deployment-target }} \
          DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
          CODE_SIGN_STYLE=Manual \
          CODE_SIGN_IDENTITY="Developer ID Application"
